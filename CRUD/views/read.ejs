<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/index.css">
    <script src="https://cdn.tailwindcss.com"></script>


</head>

<body>
    <div class="w-full h-full flex flex-col justify-center item-center p-10 text-white bg-zinc-800 min-h-screen">
        <a class="text-3xl text-red-600" href="/"> HOME</a>
        <h1 class="text-5xl my-4">All User</h1>
        <div class="users  flex gap-5  sm:justify-center s,sm:item-center flex-wrap">
           <% if(users.length>0){%>
            <% users.forEach(function(user){ %>
                <div class="user  w-72 p-4 bg-zinc-700 rounded-lg">
                    <div class="w-full overflow-hidden  h-52 rounded-xl bg-zinc-500">
                        <img class="h-full w-full object-cover" src="<%=user.image %>" alt="">
                    </div>
                    <h3 class="text-2xl tracking-tighter"><%= user.name %></h3>
                    <h5><%= user.email %></h5>
                    <div class="mt-5 flex justify-between ">
                        <a class="text-zinc-300 " href="/edit/<%= user._id%>/">EDIT USER</a>
                        <a class="text-red-600 " href="/delete/<%= user._id %>">DELETE USER</a>
                    </div>
                </div>
                <% }) %>

            <% } else{ %>
                <p class="text-2xl text-zinc-600">No User Found</p>
                <%

           } %>

        </div>
    </div>
</body>

</html>